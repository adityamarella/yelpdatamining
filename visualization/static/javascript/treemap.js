// Generated by CoffeeScript 1.7.1
(function() {
  var adjNounCloud, adjNounDataTable, categoryDataColumns, categoryDataTable, getSelectedCategoryId, selectedCategoryId, treemap, updateAdjNoun;

  categoryDataTable = null;

  categoryDataColumns = {};

  adjNounDataTable = null;

  selectedCategoryId = -1;

  treemap = null;

  adjNounCloud = null;

  google.load("visualization", "1", {
    packages: ["treemap"]
  });

  google.setOnLoadCallback(function() {
    var treemapView;
    categoryDataTable = google.visualization.arrayToDataTable(data);
    data[0].forEach(function(value, index) {
      return categoryDataColumns[value] = index;
    });
    treemapView = new google.visualization.DataView(categoryDataTable);
    treemapView.setColumns([categoryDataColumns.category_title, categoryDataColumns.parent_category, categoryDataColumns.business_count, categoryDataColumns.avg_review_count]);
    treemap = new google.visualization.TreeMap(document.getElementById('treemap-chart'));
    treemap.draw(treemapView, {
      fontColor: '#000000',
      fontSize: 16,
      maxColorValue: 100,
      minColorValue: 0,
      showScale: true
    });
    adjNounCloud = new TermCloud(document.getElementById('adjnoun-tc'));
    getSelectedCategoryId();
    updateAdjNoun(selectedCategoryId);
    return google.visualization.events.addListener(treemap, 'select', function() {
      getSelectedCategoryId();
      return console.log({
        id: categoryDataTable.getValue(selectedCategoryId, categoryDataColumns.category_id),
        title: categoryDataTable.getValue(selectedCategoryId, categoryDataColumns.category_title)
      });
    });
  });

  getSelectedCategoryId = function() {
    var selection, _ref, _ref1;
    selection = treemap.getSelection();
    selectedCategoryId = (_ref = (_ref1 = selection[0]) != null ? _ref1.row : void 0) != null ? _ref : -1;
    return selectedCategoryId;
  };

  updateAdjNoun = function(categoryId) {
    var adjNounDataView, jsonData, url;
    url = "http://amarella-project-data.appspot.com/getinfo?mode=adjectivenoun&format=json&category=1";
    jsonData = $.ajax({
      url: url,
      dataType: "json",
      async: false
    }).responseText;
    adjNounDataTable = new google.visualization.DataTable(jsonData);
    adjNounDataView = new google.visualization.DataView(adjNounDataTable);
    adjNounDataView.setColumns([1, 2]);
    return adjNounCloud.draw(adjNounDataView, null);
  };

}).call(this);
